class note {
    constructor(idNote, titleNote  ,content,dateCreate ) {
            this.idNote = idNote;
            this.titleNote = titleNote;
            this.content = content;
            this.dateCreate=dateCreate

    }
}

module.exports = note;